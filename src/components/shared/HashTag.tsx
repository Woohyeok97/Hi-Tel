import styled from "@emotion/styled";
import { colors } from "styles/colors";
import { fontSizeMap } from "styles/fontSize";

export const HashTag = styled.span`
  font-size: ${fontSizeMap['sm']};
  font-weight: 600;
  color: ${colors.blueLigth};
`;
